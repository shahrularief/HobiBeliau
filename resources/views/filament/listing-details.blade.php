<div class="space-y-4 text-gray-950 dark:text-white"><h3>{{ $listing->title }} · RM {{ $listing->price }}</h3><p>{{ $listing->game }} · {{ $listing->set_name }} · {{ $listing->condition }}</p><p class="whitespace-pre-line">{{ $listing->description }}</p><p>{{ $listing->shipping_details }}</p>@foreach($listing->images as $image)<img src="{{ Storage::disk('public')->url($image) }}" alt="{{ $listing->title }}" class="max-h-64 object-contain">@endforeach</div>
