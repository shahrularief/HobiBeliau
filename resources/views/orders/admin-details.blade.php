<div class="space-y-4 text-gray-950 dark:text-white"><p>Order #{{ $order->id }} · {{ $order->status }} · No payment collected</p><p>{{ $order->recipient }} · {{ $order->phone }}</p><p class="whitespace-pre-line">{{ $order->address }}</p>@foreach($order->items as $item)<p>{{ $item->title }} · {{ $item->condition }} · {{ $item->quantity }} × RM {{ number_format($item->unit_price_cents / 100, 2) }}</p>@endforeach<p>Total: RM {{ number_format($order->total_cents / 100, 2) }}</p><p>Tracking: {{ $order->tracking ?? 'Not shipped' }}</p></div>
